import { XplatHelpers } from '@nstudio/xplat';
import { chain, noop } from '@angular-devkit/schematics';

export default function(options: XplatHelpers.Schema) {
  // TODO
  console.log('Vanilla Ionic app generator is coming soon.');
  return chain([noop()]);
}
