{
  "$schema": "http://json-schema.org/schema",
  "id": "ts-config",
  "title": "tsconfig for xplat schema",
  "type": "object",
  "properties": {
    "platforms": {
      "type": "string",
      "description": "Target platforms to configure.",
      "default": "web,nativescript"
    }
  }
}
